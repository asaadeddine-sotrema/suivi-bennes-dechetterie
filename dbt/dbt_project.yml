name: 'sotrema_bennes'
version: '1.0.0'
config-version: 2

# Profil de connexion (voir profiles.yml dans ce dossier).
profile: 'sotrema_bennes'

model-paths: ["models"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]

models:
  sotrema_bennes:
    # Couche staging : nettoyage/renommage 1:1 des tables sources -> vues.
    staging:
      +materialized: view
    # Couche marts : modèles analytiques (faits/dimensions) -> tables.
    marts:
      +materialized: table
